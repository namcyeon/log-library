# Quarkus VS Log Library


### Dependency
- maven:

```xml
<dependency>
      <groupId>com.vs.quarkus.extensions</groupId>
      <artifactId>log</artifactId>
      <version>0.0.1</version>
</dependency>
```
### Configuration
```xml
nats.servers=[ ]
nats.topic.log=string
```
